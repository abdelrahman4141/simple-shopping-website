<?php include 'define.php';


$conn =mysqli_connect(DB_HOST,DB_USER,DB_PASS,DB_NAME) or die('connection failed');

//if the connection fails, it will output 'connection failed' and stop the script execution.

